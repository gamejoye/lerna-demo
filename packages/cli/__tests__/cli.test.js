// import cli from '../src/cli.js';
// require('../src/cli.js')

describe('cli cli', () => {
  // const argv = cli(cwd).parse(['args']);
  it('needs tests', () => {
    expect('1' + '2' + '3').toBe('123')
  });
});
